---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mitmproxy-data
  namespace: mitmproxy
  labels:
    app: mitmproxy
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uncomment and set your storage class if needed
  # storageClassName: local-path
