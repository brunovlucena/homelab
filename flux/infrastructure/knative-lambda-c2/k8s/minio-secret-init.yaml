# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
#  🔐 MINIO SECRET INIT JOB
#
#  Purpose: Automatically copies minio-credentials secret from minio namespace
#           to knative-lambda-c2 namespace during bootstrapping
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
---
apiVersion: batch/v1
kind: Job
metadata:
  name: minio-secret-init
  namespace: knative-lambda-c2
  labels:
    app.kubernetes.io/name: minio-secret-init
    app.kubernetes.io/component: bootstrap
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: minio-secret-init
    spec:
      restartPolicy: OnFailure
      serviceAccountName: c2-secret-init
      containers:
        - name: copy-secret
          image: localhost:5001/kubectl:v1.34.0
          command:
            - sh
            - -c
            - |
              set -e
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo "🔐 MinIO Secret Bootstrap for C2"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo ""
              
              # Wait for minio namespace and secret to exist
              echo "⏳ Waiting for minio namespace..."
              until kubectl get namespace minio &>/dev/null; do
                echo "   MinIO namespace not found, waiting..."
                sleep 2
              done
              echo "✅ MinIO namespace found"
              
              echo ""
              echo "⏳ Waiting for minio-credentials secret in minio namespace..."
              until kubectl get secret minio-credentials -n minio &>/dev/null; do
                echo "   Secret not found, waiting..."
                sleep 2
              done
              echo "✅ Secret found in minio namespace"
              
              echo ""
              echo "📋 Checking if secret exists in knative-lambda-c2 namespace..."
              if kubectl get secret minio-credentials -n knative-lambda-c2 &>/dev/null; then
                echo "✅ Secret already exists in knative-lambda-c2 namespace"
                echo "   Verifying it's up to date..."
                
                kubectl get secret minio-credentials -n minio -o yaml | \
                  sed 's/namespace: minio/namespace: knative-lambda-c2/' | \
                  sed '/^  uid:/d' | \
                  sed '/^  resourceVersion:/d' | \
                  sed '/^  creationTimestamp:/d' | \
                  kubectl apply -f -
                echo "✅ Secret updated/verified"
              else
                echo "📤 Copying secret from minio namespace..."
                kubectl get secret minio-credentials -n minio -o yaml | \
                  sed 's/namespace: minio/namespace: knative-lambda-c2/' | \
                  sed '/^  uid:/d' | \
                  sed '/^  resourceVersion:/d' | \
                  sed '/^  creationTimestamp:/d' | \
                  kubectl apply -f -
                echo "✅ Secret copied successfully"
              fi
              
              echo ""
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo "✅ MinIO secret bootstrap complete!"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
