# Kustomize configuration for nameReference
# Ensures ConfigMap references in Jobs are updated when ConfigMap name changes
nameReference:
  - kind: ConfigMap
    version: v1
    fieldSpecs:
      - path: spec/template/spec/volumes/configMap/name
        kind: Job
