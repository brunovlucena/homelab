---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Knative Operator - Installs CRDs first via HelmRelease
# Note: KnativeServing and KnativeEventing are applied separately
# after the operator CRDs are installed (see knative-serving/kustomization.yaml)

resources:
  - namespace.yaml
  - helmrelease.yaml