apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Don't apply global namespace transformation to avoid conflicts
# Instead, each resource specifies its own namespace

resources:
  - namespace.yaml
  - install-job.yaml
  - install-rbac.yaml