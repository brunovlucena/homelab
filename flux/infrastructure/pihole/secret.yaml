---
# NOTE: Replace this with a sealed-secret or external-secrets for production
# This is a placeholder - the password should be managed securely
apiVersion: v1
kind: Secret
metadata:
  name: pihole-secret
  namespace: pihole
type: Opaque
stringData:
  # Change this password before deploying!
  FTLCONF_webserver_api_password: "change-me-in-production"
