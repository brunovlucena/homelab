---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: github-runners-secret-sync
  namespace: github-runners
  labels:
    app: github-runners
    component: secret-sync
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: github-runners-secret-sync
  labels:
    app: github-runners
    component: secret-sync
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list", "create", "update", "patch"]
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: github-runners-secret-sync
  labels:
    app: github-runners
    component: secret-sync
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: github-runners-secret-sync
subjects:
- kind: ServiceAccount
  name: github-runners-secret-sync
  namespace: github-runners
---
apiVersion: batch/v1
kind: Job
metadata:
  name: github-runners-secret-sync
  namespace: github-runners
  labels:
    app: github-runners
    component: secret-sync
  annotations:
    # Prevent Flux from trying to reconcile this Job after creation
    # Jobs have immutable spec.template, so reconciliation will fail
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app: github-runners
        component: secret-sync
    spec:
      serviceAccountName: github-runners-secret-sync
      restartPolicy: Never
      containers:
      - name: sync
        image: localhost:5001/kubectl:v1.34.0
        imagePullPolicy: IfNotPresent
        env:
        - name: SOURCE_NAMESPACE
          value: flux-system
        - name: TARGET_NAMESPACE
          value: github-runners
        - name: SECRET_NAMES
          value: "github-token"
        command:
        - /bin/bash
        - -c
        - |
          set -euo pipefail

          echo "Syncing secrets into ${TARGET_NAMESPACE}"
          for secret in ${SECRET_NAMES}; do
            echo "Processing ${secret}"
            # Get the secret and transform it for ARC compatibility
            # ARC expects 'github_token' key, but Flux uses 'password' key
            kubectl get secret "${secret}" -n "${SOURCE_NAMESPACE}" -o json \
              | jq --arg target "${TARGET_NAMESPACE}" '
                  .metadata.namespace = $target
                  | .metadata.labels = (.metadata.labels // {})
                  | .metadata.annotations = (.metadata.annotations // {})
                  | del(.metadata.uid,
                        .metadata.resourceVersion,
                        .metadata.creationTimestamp,
                        .metadata.managedFields,
                        .metadata.ownerReferences)
                  | .data.github_token = (.data.password // .data.github_token)
                ' \
              | kubectl apply -f -
          done
          
          echo "Forcing rollout of all deployments in ${TARGET_NAMESPACE}"
          for deployment in $(kubectl get deployments -n "${TARGET_NAMESPACE}" -o jsonpath='{.items[*].metadata.name}'); do
            echo "Rolling out deployment ${deployment}"
            kubectl rollout restart deployment "${deployment}" -n "${TARGET_NAMESPACE}"
          done
          
          echo "Waiting for rollouts to complete"
          for deployment in $(kubectl get deployments -n "${TARGET_NAMESPACE}" -o jsonpath='{.items[*].metadata.name}'); do
            echo "Waiting for deployment ${deployment} to be ready"
            kubectl rollout status deployment "${deployment}" -n "${TARGET_NAMESPACE}" --timeout=5m || echo "Warning: Deployment ${deployment} rollout did not complete within timeout"
          done
          
          echo "✅ Secret sync and rollout complete"

