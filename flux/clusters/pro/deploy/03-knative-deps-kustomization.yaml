---
# 🚀 Phase 3: Knative Dependencies (runs in PARALLEL with 02-observability)
# Both depend only on 01-core, enabling parallel deployment
# Estimated time: ~2min (parallel with phase 2)
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: pro-03-knative-deps
  namespace: flux-system
spec:
  interval: 5m
  retryInterval: 30s
  sourceRef:
    kind: GitRepository
    name: homelab
  path: ./flux/clusters/pro/deploy/03-knative-deps
  prune: true
  wait: true
  timeout: 5m
  dependsOn:
    - name: pro-01-core

