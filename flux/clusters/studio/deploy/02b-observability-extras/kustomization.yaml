---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📊 Studio Cluster - Observability Extras Phase
# ServiceMonitors, AlertmanagerConfigs, PrometheusRules, and other resources that depend on Prometheus CRDs
# Must run AFTER kube-prometheus-stack HelmRelease is deployed and CRDs are available

resources: []
