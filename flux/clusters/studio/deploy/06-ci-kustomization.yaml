---
# 🔄 Phase 6: CI/CD (runs in PARALLEL with 05-testing after phase 4)
# Estimated time: ~30s (parallel with phase 5)
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: studio-06-ci
  namespace: flux-system
spec:
  interval: 5m
  retryInterval: 30s
  sourceRef:
    kind: GitRepository
    name: homelab
  path: ./flux/clusters/studio/deploy/06-ci
  prune: true
  wait: true
  timeout: 3m

