---
# ⚡ Phase 4: Knative Instances (waits for phase 3 only)
# Observability dependency removed - observability shall not block deployments
# Estimated time: ~1min
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: studio-04-knative-instances
  namespace: flux-system
spec:
  interval: 5m
  retryInterval: 30s
  sourceRef:
    kind: GitRepository
    name: homelab
  path: ./flux/clusters/studio/deploy/04-knative-instances
  prune: true
  wait: true
  timeout: 5m
  dependsOn:
    - name: studio-03-knative-deps

