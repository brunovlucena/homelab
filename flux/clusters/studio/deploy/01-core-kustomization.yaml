---
# 🏗️ Phase 1: Core Infrastructure (runs first, no dependencies)
# Estimated time: ~2-3min
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: studio-01-core
  namespace: flux-system
spec:
  interval: 5m
  retryInterval: 30s
  sourceRef:
    kind: GitRepository
    name: homelab
  path: ./flux/clusters/studio/deploy/01-core
  prune: true
  wait: true
  timeout: 5m

