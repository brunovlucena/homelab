---
# 🤖 Phase 8: AI/ML Infrastructure (depends on core infrastructure and data stores)
# Estimated time: ~2-3min
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: studio-08-ai
  namespace: flux-system
spec:
  interval: 5m
  retryInterval: 30s
  sourceRef:
    kind: GitRepository
    name: homelab
  path: ./flux/clusters/studio/deploy/08-ai
  prune: true
  wait: true
  timeout: 5m
  dependsOn:
    - name: studio-01-core  # Needs minio, postgres

