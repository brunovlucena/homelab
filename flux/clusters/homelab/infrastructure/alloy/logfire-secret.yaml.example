# Example Kubernetes Secret for Logfire Token
# 
# To use Logfire SaaS, you need to:
# 1. Sign up at https://logfire.pydantic.dev
# 2. Get your API token from the Logfire dashboard
# 3. Create this secret in your cluster
#
# Create the secret using:
#   kubectl create secret generic logfire-secrets \
#     --from-literal=token="Bearer YOUR_LOGFIRE_TOKEN_HERE" \
#     -n alloy
#
# Or using sealed-secrets (recommended):
#   kubectl create secret generic logfire-secrets \
#     --from-literal=token="Bearer YOUR_LOGFIRE_TOKEN_HERE" \
#     --dry-run=client -o yaml | \
#     kubeseal --format=yaml > logfire-sealed-secret.yaml

---
apiVersion: v1
kind: Secret
metadata:
  name: logfire-secrets
  namespace: alloy
type: Opaque
stringData:
  # Get your token from https://logfire.pydantic.dev
  # Format: "Bearer YOUR_TOKEN_HERE"
  token: "Bearer YOUR_LOGFIRE_TOKEN_GOES_HERE"

