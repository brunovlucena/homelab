apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: prometheus-mcp
  namespace: prometheus-mcp

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - knative-service.yaml

