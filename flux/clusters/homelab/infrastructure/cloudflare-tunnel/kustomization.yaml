apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: cloudflare-tunnel
  namespace: flux-system

resources:
  - deployment.yaml
  - service.yaml
  - prometheus-rule.yaml
  - servicemonitor.yaml
  # Temporarily disable canary-monitoring until Prometheus operator is ready
  # - canary-monitoring.yaml