apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: cloudflare-tunnel
  namespace: flux-system

resources:
  - deployment.yaml
  - service.yaml
  # Prometheus monitoring resources moved to phase 2 (after prometheus-operator CRDs are ready)
  # - prometheus-rule.yaml
  # - servicemonitor.yaml
  # - canary-monitoring.yaml