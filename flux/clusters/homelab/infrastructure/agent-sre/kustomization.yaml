apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: sre-agent

resources:
  - namespace.yaml
  - deployments/agent
  - deployments/mcp-server

commonLabels:
  app: sre-agent
  version: v0.2.0
  managed-by: flux

namespace: agent-sre
