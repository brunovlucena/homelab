
apiVersion: v1
kind: Service
metadata:
  name: {{ include "homepage.fullname" . }}-api-nodeport
  labels:
    {{- include "homepage.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: NodePort
  ports:
  - name: http
    port: 8080
    targetPort: http
    nodePort: 30110
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: metrics
    nodePort: 30111
    protocol: TCP
  selector:
    {{- include "homepage.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
