apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sealed-secrets
  namespace: sealed-secrets

resources:
  - namespace.yaml
  - helmrelease.yaml
  # Note: rbac.yaml removed - RBAC is managed by the Helm chart
