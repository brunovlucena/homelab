apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # - sealed-secret.yaml  # Consolidated into minio-secrets.yaml in infrastructure/secrets/
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - setup-job.yaml

images:
  - name: minio/minio
    newTag: latest
  - name: minio/mc
    newTag: latest
