apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-gateway-data
  namespace: mcp-gateway
  labels:
    app.kubernetes.io/name: mcp-gateway
    app.kubernetes.io/component: mcp-adapter
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # For Kind cluster, use hostPath storage class (default)
  # storageClassName: standard

