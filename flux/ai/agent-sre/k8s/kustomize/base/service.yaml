apiVersion: v1
kind: Service
metadata:
  name: agent-sre
  labels:
    app: agent-sre
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: agent-sre

