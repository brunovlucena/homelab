apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai

resources:
  - rbac.yaml
  - secrets-job.yaml  # Creates medical-db-credentials secret
  - lambdaagent.yaml
  - nodeport.yaml

commonLabels:
  app.kubernetes.io/name: agent-medical
  app.kubernetes.io/part-of: homelab
