---
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Medical Agent - Web Command Center Deployment
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
apiVersion: apps/v1
kind: Deployment
metadata:
  name: medical-command-center
  namespace: agent-medical
  labels:
    app.kubernetes.io/name: medical-command-center
    app.kubernetes.io/part-of: agent-medical
    app.kubernetes.io/component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: medical-command-center
  template:
    metadata:
      labels:
        app.kubernetes.io/name: medical-command-center
        app.kubernetes.io/part-of: agent-medical
        app.kubernetes.io/component: web
    spec:
      containers:
        - name: command-center
          image: localhost:5001/agent-medical/command-center:1.0.0
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: AGENT_MEDICAL_URL
              value: "http://agent-medical.agent-medical.svc.cluster.local:8080"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: medical-command-center
  namespace: agent-medical
  labels:
    app.kubernetes.io/name: medical-command-center
    app.kubernetes.io/part-of: agent-medical
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: medical-command-center
