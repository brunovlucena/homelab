# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
#  📱 MAX - AI Seller Tech
#
#  Tech enthusiast specializing in electronics and gadgets
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
---
apiVersion: lambda.knative.io/v1alpha1
kind: LambdaAgent
metadata:
  name: ai-seller-tech
  namespace: ai
  labels:
    app.kubernetes.io/name: ai-seller-tech
    app.kubernetes.io/component: ai-seller
    app.kubernetes.io/part-of: agent-store-multibrands
    store.agent.role: seller
    store.agent.brand: tech
  annotations:
    description: "MAX - AI Tech Specialist"
spec:
  serviceAccountName: store-agent-sa

  image:
    repository: localhost:5001/ai_seller
    tag: "0.3.0"
    port: 8080

  ai:
    provider: ollama
    endpoint: "http://ollama-native.ollama.svc.cluster.local:11434"
    model: "llama3.2:3b"
    maxTokens: 1024
    temperature: "0.7"

  behavior:
    maxContextMessages: 15
    emitEvents: true
    systemPrompt: |
      Você é MAX, um especialista em tecnologia apaixonado.
      
      PERSONALIDADE:
      - Conhecedor profundo de especificações técnicas
      - Entusiasta de novidades tecnológicas
      - Objetivo e prático nas recomendações
      - Usa emojis tech com moderação (📱💻🎧)
      
      Sempre responda em português do Brasil e seja prestativo!

  scaling:
    minReplicas: 0
    maxReplicas: 10
    targetConcurrency: 5
    scaleToZeroGracePeriod: 120s

  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"

  env:
    - name: AGENT_NAME
      value: "Max"
    - name: AGENT_BRAND
      value: "tech"

  eventing:
    enabled: true
    eventSource: "/agent-store-multibrands/ai-seller/tech"
    
    intents:
      - store.chat.response
      - store.product.query
      - store.product.recommend
      - store.cart.item.added
      - store.order.create
      - store.sales.escalate
    
    subscriptions:
      - eventType: store.chat.message.new
        description: "New customer messages for tech brand"
      - eventType: store.product.query.result
        description: "Product search results"
      - eventType: store.product.recommend.result
        description: "Product recommendations"

    dlq:
      enabled: true
      retryMaxAttempts: 3

    rabbitmq:
      clusterName: rabbitmq-cluster-knative-lambda
      namespace: knative-lambda

  observability:
    tracing:
      enabled: true
      endpoint: alloy.observability.svc:4317
    metrics:
      enabled: true
    logging:
      level: info
      format: json

  permissions:
    disableBrokerCreation: true
    disableTriggerCreation: true
    disableFunctionCreation: true
