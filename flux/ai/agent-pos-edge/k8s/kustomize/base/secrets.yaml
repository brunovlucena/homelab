# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
#  🔐 Secrets Template for Agent-POS-Edge
#
#  Note: Replace with SealedSecrets in production
#  These are placeholder values - DO NOT use in production!
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
---
apiVersion: v1
kind: Secret
metadata:
  name: pos-secrets
  namespace: ai
  labels:
    app.kubernetes.io/name: agent-pos-edge
    app.kubernetes.io/component: secrets
    app.kubernetes.io/part-of: agent-pos-edge
type: Opaque
stringData:
  # Alert Webhook URL for command-center notifications
  alert-webhook-url: "https://hooks.slack.com/services/CHANGE_ME"
