apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: homelab-services

resources:
  - ../../base

# Production overlay - can add production-specific configs here
# For now, uses same images as base (localhost registry for local dev)
