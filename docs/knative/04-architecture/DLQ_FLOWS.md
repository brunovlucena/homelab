# 💀 Dead Letter Queue (DLQ) Flow Diagrams

**Complete ASCII flow diagrams for DLQ handling in Knative Lambda**

---

## 📊 Flow 1: Normal Event Processing (Success Path)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 1. EVENT INGESTION                                                       │
└─────────────────────────────────────────────────────────────────────────┘

External System
    │
    │ CloudEvent (type: network.notifi.lambda.build.start)
    │ POST /api/events
    ↓
┌──────────────────────────────────┐
│  Knative Broker                  │
│  (RabbitMQ Broker)               │
│  ─────────────────────            │
│  Exchange: knative-broker        │
│  Type: topic                     │
│  Durable: true                   │
└──────────────────────────────────┘
    │
    │ Routes event based on type
    ↓
┌──────────────────────────────────┐
│  Knative Trigger                  │
│  ─────────────────────            │
│  Name: knative-lambda-builder    │
│  Filter:                         │
│    type: network.notifi.lambda.   │
│         build.start               │
│                                   │
│  Subscriber:                     │
│    knative-lambda-builder-ksvc    │
└──────────────────────────────────┘
    │
    │ Delivery attempt #1
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  (Knative Service)                │
│  ─────────────────────            │
│  Pod: builder-xxx-00001           │
│  Endpoint: POST /cloud-events     │
└──────────────────────────────────┘
    │
    │ Process event successfully
    │ Status: 200 OK
    ↓
┌──────────────────────────────────┐
│  ✅ SUCCESS                       │
│  Event processed                  │
│  No retries needed                │
└──────────────────────────────────┘
```

---

## 🔄 Flow 2: Knative Trigger Retry Flow (Before DLQ)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 2. EVENT PROCESSING WITH RETRIES                                        │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│  Knative Trigger                  │
│  ─────────────────────            │
│  delivery:                        │
│    retry: 5                       │
│    backoffPolicy: exponential     │
│    backoffDelay: PT1S             │
│    timeout: PT30S                 │
└──────────────────────────────────┘
    │
    │ Attempt #1: Immediate
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  POST /cloud-events                │
│  Timeout: 30s                     │
└──────────────────────────────────┘
    │
    │ ❌ FAILURE (500 Internal Error)
    │ or ⏱️ TIMEOUT (30s exceeded)
    ↓
┌──────────────────────────────────┐
│  Knative Trigger Retry Logic     │
│  ─────────────────────            │
│  Wait: 1s (backoff)               │
└──────────────────────────────────┘
    │
    │ Attempt #2: After 1s
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  POST /cloud-events                │
└──────────────────────────────────┘
    │
    │ ❌ FAILURE
    ↓
┌──────────────────────────────────┐
│  Knative Trigger Retry Logic     │
│  ─────────────────────            │
│  Wait: 2s (exponential backoff)   │
└──────────────────────────────────┘
    │
    │ Attempt #3: After 3s total
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  POST /cloud-events                │
└──────────────────────────────────┘
    │
    │ ❌ FAILURE
    ↓
┌──────────────────────────────────┐
│  Knative Trigger Retry Logic     │
│  ─────────────────────            │
│  Wait: 4s (exponential backoff)   │
└──────────────────────────────────┘
    │
    │ Attempt #4: After 7s total
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  POST /cloud-events                │
└──────────────────────────────────┘
    │
    │ ❌ FAILURE
    ↓
┌──────────────────────────────────┐
│  Knative Trigger Retry Logic     │
│  ─────────────────────            │
│  Wait: 8s (exponential backoff)     │
└──────────────────────────────────┘
    │
    │ Attempt #5: After 15s total (FINAL)
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  POST /cloud-events                │
└──────────────────────────────────┘
    │
    │ ❌ FAILURE (All retries exhausted)
    ↓
┌──────────────────────────────────┐
│  ⚠️ ALL RETRIES EXHAUSTED         │
│  Move to DLQ                      │
└──────────────────────────────────┘
```

---

## 💀 Flow 3: DLQ Routing (Knative Broker → DLQ)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 3. DLQ ROUTING (After All Retries Fail)                                │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│  Knative Broker                   │
│  (RabbitMQ Broker)                │
│  ─────────────────────            │
│  deadLetterSink configured:      │
│    Service:                       │
│      knative-lambda-dlq-handler   │
└──────────────────────────────────┘
    │
    │ Event failed after 5 retries
    │ Add DLQ headers:
    │   - x-death-reason: retry_exhausted
    │   - x-death-count: 5
    │   - x-original-routing-key: network.notifi.lambda.build.start
    ↓
┌──────────────────────────────────┐
│  RabbitMQ DLQ Exchange           │
│  ─────────────────────            │
│  Name: knative-lambda-dlq        │
│  Type: topic                     │
│  Durable: true                   │
│  AutoDelete: false               │
└──────────────────────────────────┘
    │
    │ Routing key: dlq.build.start
    │ (from dlq.routingKeyPrefix.#)
    ↓
┌──────────────────────────────────┐
│  RabbitMQ DLQ Binding            │
│  ─────────────────────            │
│  Source: knative-lambda-dlq      │
│  Destination: lambda-dlq-queue  │
│  RoutingKey: dlq.#               │
└──────────────────────────────────┘
    │
    │ Route message to DLQ queue
    ↓
┌──────────────────────────────────┐
│  RabbitMQ DLQ Queue              │
│  ─────────────────────            │
│  Name: lambda-dlq-queue-{env}     │
│  Type: quorum                    │
│  Durable: true                   │
│  Arguments:                      │
│    x-message-ttl: 604800000      │
│      (7 days)                    │
│    x-max-length: 50000           │
│    x-overflow: reject-publish    │
│    x-queue-type: quorum          │
└──────────────────────────────────┘
    │
    │ Message stored in DLQ
    │ Metadata preserved:
    │   - Original event
    │   - Failure reason
    │   - Retry count
    │   - Timestamp
    │   - Error details
    ↓
┌──────────────────────────────────┐
│  📊 Metrics Emitted              │
│  ─────────────────────            │
│  - dlq_messages_total             │
│  - dlq_message_age_seconds        │
│  - dlq_depth_gauge                │
└──────────────────────────────────┘
    │
    │ Alert if threshold exceeded
    ↓
┌──────────────────────────────────┐
│  🚨 Prometheus Alert              │
│  ─────────────────────            │
│  Alert: DLQAboveThreshold         │
│  Fires when:                     │
│    dlq_depth > 100 for 10m       │
└──────────────────────────────────┘
```

---

## 🔄 Flow 4: DLQ Handler Processing

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 4. DLQ HANDLER PROCESSING                                               │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│  RabbitMQ DLQ Queue              │
│  ─────────────────────            │
│  Message waiting                 │
└──────────────────────────────────┘
    │
    │ Consumer: DLQ Handler Service
    ↓
┌──────────────────────────────────┐
│  DLQ Handler Service              │
│  (Kubernetes Service)          │
│  ─────────────────────              │
│  Endpoint: /dlq/process          │
│  Consumes from:                  │
│    lambda-dlq-queue-{env}         │
└──────────────────────────────────┘
    │
    │ 1. Receive message
    ↓
┌──────────────────────────────────┐
│  DLQ Message Processing          │
│  ─────────────────────            │
│  Steps:                          │
│    1. Parse message              │
│    2. Extract metadata           │
│    3. Categorize failure         │
│       - Permanent?               │
│       - Transient?               │
│    4. Update statistics          │
│    5. Record metrics             │
└──────────────────────────────────┘
    │
    │ 2. Categorize failure
    ↓
┌──────────────────────────────────┐
│  Failure Categorization          │
│  ─────────────────────            │
│  Categories:                     │
│  ─────────────────────            │
│  PERMANENT (no retry):           │
│    - broker_auth                 │
│    - s3_access_denied            │
│    - service_misconfigured        │
│                                   │
│  TRANSIENT (can retry):          │
│    - subscriber_down             │
│    - build_timeout               │
│    - image_pull_error             │
│    - queue_full                   │
└──────────────────────────────────┘
    │
    │ 3. Update statistics
    ↓
┌──────────────────────────────────┐
│  DLQ Statistics                  │
│  ─────────────────────            │
│  - Total messages                │
│  - By category                   │
│  - By permanence                 │
│  - Average age                   │
│  - Oldest message age            │
└──────────────────────────────────┘
    │
    │ 4. Check alert threshold
    ↓
┌──────────────────────────────────┐
│  Alert Check                     │
│  ─────────────────────            │
│  If message age > threshold:     │
│    → Emit alert                  │
│    → Log permanent failure       │
└──────────────────────────────────┘
    │
    │ 5. Message processed
    │    (stays in queue for replay)
    ↓
┌──────────────────────────────────┐
│  ✅ DLQ Message Processed         │
│  ─────────────────────            │
│  Message remains in queue        │
│  Available for replay            │
└──────────────────────────────────┘
```

---

## 🔄 Flow 5: DLQ Replay Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 5. DLQ REPLAY (Manual or Automated)                                     │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│  DLQ Replay Trigger              │
│  ─────────────────────            │
│  Options:                        │
│    1. Manual (API)                │
│    2. Automated (CronJob)         │
│    3. RabbitMQ Shovel             │
└──────────────────────────────────┘
    │
    │ Option 1: Manual API
    ↓
┌──────────────────────────────────┐
│  DLQ Handler API                 │
│  ─────────────────────            │
│  POST /api/dlq/replay            │
│  Query params:                   │
│    - count: 10                   │
│    - filter: category=transient  │
│    - maxAge: 24h                 │
└──────────────────────────────────┘
    │
    │ Option 2: Automated CronJob
    ↓
┌──────────────────────────────────┐
│  DLQ Replay CronJob              │
│  ─────────────────────            │
│  Schedule: */5 * * * *          │
│  (Every 5 minutes)               │
│                                   │
│  Steps:                          │
│    1. Check DLQ depth            │
│    2. Verify consumer health     │
│    3. Replay batch (10 msgs)     │
│    4. Rate limit (100ms delay)   │
└──────────────────────────────────┘
    │
    │ Get messages from DLQ
    ↓
┌──────────────────────────────────┐
│  RabbitMQ DLQ Queue              │
│  ─────────────────────            │
│  Get messages (requeue=false)    │
│  Filter:                         │
│    - Age < 24h                   │
│    - Category = transient        │
└──────────────────────────────────┘
    │
    │ For each message:
    ↓
┌──────────────────────────────────┐
│  Replay Message                  │
│  ─────────────────────            │
│  Steps:                          │
│    1. Extract original event     │
│    2. Restore headers            │
│    3. Remove DLQ headers         │
│    4. Publish to main broker     │
└──────────────────────────────────┘
    │
    │ Publish to main broker
    ↓
┌──────────────────────────────────┐
│  Knative Broker                   │
│  (RabbitMQ Broker)                │
│  ─────────────────────            │
│  Exchange: knative-broker        │
│  Routing: Original routing key    │
└──────────────────────────────────┘
    │
    │ Event re-enters normal flow
    ↓
┌──────────────────────────────────┐
│  Knative Trigger                  │
│  ─────────────────────            │
│  Routes to Builder Service       │
└──────────────────────────────────┘
    │
    │ Process event again
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  Process event                    │
│  (with fresh retry count)        │
└──────────────────────────────────┘
    │
    │ ✅ SUCCESS or ❌ FAILURE
    │    (if failure, may go to DLQ again)
    ↓
┌──────────────────────────────────┐
│  Replay Metrics                  │
│  ─────────────────────            │
│  - dlq_replay_total               │
│  - dlq_replay_success_total        │
│  - dlq_replay_failure_total        │
└──────────────────────────────────┘
```

---

## 🔀 Flow 6: Complete Failure Scenarios

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 6. FAILURE SCENARIOS & DLQ ROUTING                                      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ SCENARIO 1: Poison Message (Schema Validation Failure)              │
└─────────────────────────────────────────────────────────────────────┘

Event arrives
    │
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  Parse CloudEvent                 │
└──────────────────────────────────┘
    │
    │ ❌ JSON Parse Error
    │    Missing required field: data.contextId
    ↓
┌──────────────────────────────────┐
│  Error Interceptor                │
│  ─────────────────────            │
│  InterceptBuildError()            │
│  Category: validation             │
│  Permanence: PERMANENT            │
└──────────────────────────────────┘
    │
    │ Immediately to DLQ
    │ (no retries for poison messages)
    ↓
┌──────────────────────────────────┐
│  RabbitMQ DLQ Queue              │
│  ─────────────────────            │
│  Message stored with:             │
│    - Original malformed event     │
│    - Error: validation_failure     │
│    - Category: validation         │
└──────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ SCENARIO 2: Consumer Unavailable (Service Down)                     │
└─────────────────────────────────────────────────────────────────────┘

Event arrives
    │
    ↓
┌──────────────────────────────────┐
│  Knative Trigger                  │
│  ─────────────────────            │
│  Attempt delivery                 │
└──────────────────────────────────┘
    │
    │ ❌ 503 Service Unavailable
    │    Builder Service scaled to zero
    │    or pod crashed
    ↓
┌──────────────────────────────────┐
│  Retry Attempt #1                │
│  Wait: 1s                         │
└──────────────────────────────────┘
    │
    │ ❌ 503 Service Unavailable
    ↓
┌──────────────────────────────────┐
│  Retry Attempt #2                │
│  Wait: 2s                         │
└──────────────────────────────────┘
    │
    │ ❌ 503 Service Unavailable
    │    ... (retries 3, 4, 5)
    ↓
┌──────────────────────────────────┐
│  All Retries Exhausted            │
│  → DLQ                            │
│                                   │
│  Category: subscriber_down        │
│  Permanence: TRANSIENT            │
└──────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ SCENARIO 3: Processing Timeout                                      │
└─────────────────────────────────────────────────────────────────────┘

Event arrives
    │
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  Processing event...              │
└──────────────────────────────────┘
    │
    │ ⏱️ Processing takes >30s
    │    (Knative timeout)
    ↓
┌──────────────────────────────────┐
│  Knative Trigger                 │
│  ─────────────────────            │
│  Timeout: 30s exceeded           │
│  → Retry                          │
└──────────────────────────────────┘
    │
    │ Retry attempts (same timeout)
    ↓
┌──────────────────────────────────┐
│  All Retries Timeout              │
│  → DLQ                            │
│                                   │
│  Category: build_timeout          │
│  Permanence: TRANSIENT            │
│  Error details: timeout_exceeded  │
└──────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ SCENARIO 4: Dependency Failure (S3/ECR Unavailable)                  │
└─────────────────────────────────────────────────────────────────────┘

Event arrives
    │
    ↓
┌──────────────────────────────────┐
│  Builder Service                  │
│  ─────────────────────            │
│  Fetch code from S3               │
└──────────────────────────────────┘
    │
    │ ❌ ECONNREFUSED
    │    S3 endpoint unreachable
    ↓
┌──────────────────────────────────┐
│  Error Interceptor                │
│  ─────────────────────            │
│  InterceptBuildError()            │
│  Category: dependency_error       │
│  Permanence: TRANSIENT            │
└──────────────────────────────────┘
    │
    │ Return error to trigger
    │ (trigger will retry)
    ↓
┌──────────────────────────────────┐
│  Knative Trigger Retries          │
│  (5 attempts)                     │
└──────────────────────────────────┘
    │
    │ All retries fail
    ↓
┌──────────────────────────────────┐
│  → DLQ                            │
│                                   │
│  Category: dependency_error       │
│  Permanence: TRANSIENT            │
│  Error: S3 connection failed      │
└──────────────────────────────────┘
```

---

## 🔄 Flow 7: RabbitMQ Native DLQ (Alternative Path)

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 7. RABBITMQ NATIVE DLQ (Alternative Implementation)                    │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│  RabbitMQ Main Queue             │
│  ─────────────────────            │
│  Name: lambda-build-events        │
│  Type: quorum                     │
│  Arguments:                      │
│    x-dead-letter-exchange:        │
│      knative-lambda-dlx           │
│    x-dead-letter-routing-key:     │
│      dlq                          │
│    x-message-ttl: 300000          │
│      (5 min per retry)            │
└──────────────────────────────────┘
    │
    │ Consumer: Builder Service
    │ Message processing fails
    │ (NACK with requeue=false)
    ↓
┌──────────────────────────────────┐
│  RabbitMQ Dead Letter Exchange   │
│  ─────────────────────            │
│  Name: knative-lambda-dlx         │
│  Type: topic                     │
│  Durable: true                   │
└──────────────────────────────────┘
    │
    │ Routing key: dlq
    ↓
┌──────────────────────────────────┐
│  DLQ Binding                     │
│  ─────────────────────            │
│  Source: knative-lambda-dlx       │
│  Destination: lambda-dlq-queue    │
│  RoutingKey: dlq                  │
└──────────────────────────────────┘
    │
    │ Route to DLQ
    ↓
┌──────────────────────────────────┐
│  RabbitMQ DLQ Queue              │
│  ─────────────────────            │
│  Message stored                  │
│  (with x-death headers)          │
└──────────────────────────────────┘

Note: Current implementation uses Knative Broker DLQ,
but RabbitMQ native DLQ can be used as fallback or alternative.
```

---

## 📊 Summary: DLQ Components Interaction

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    DLQ ARCHITECTURE OVERVIEW                            │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  Event Source   │
│  (External)     │
└────────┬────────┘
         │ CloudEvent
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│  KNATIVE BROKER (RabbitMQ Backend)                                  │
│  ─────────────────────────────────                                   │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Exchange: knative-broker                                     │  │
│  │  Queue: lambda-build-events (quorum)                          │  │
│  │  Retry Policy: 5 attempts, exponential backoff               │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  On failure after retries:                                           │
│    ↓                                                                 │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  deadLetterSink: Service → knative-lambda-dlq-handler         │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ Failed events
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│  RABBITMQ DLQ INFRASTRUCTURE                                         │
│  ─────────────────────────────────                                   │
│                                                                       │
│  ┌──────────────────┐    ┌──────────────┐    ┌──────────────────┐  │
│  │  DLQ Exchange    │───→│  DLQ Binding │───→│  DLQ Queue       │  │
│  │  (topic)         │    │              │    │  (quorum)        │  │
│  └──────────────────┘    └──────────────┘    └──────────────────┘  │
│         │                                            │                │
│         │                                            │                │
│         └──────────────────────────────────────────┘                │
│                          Routing                                       │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ Messages stored
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│  DLQ HANDLER SERVICE                                                 │
│  ─────────────────────────────────                                   │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Functions:                                                   │  │
│  │    - Consume messages                                         │  │
│  │    - Categorize failures                                      │  │
│  │    - Update statistics                                        │  │
│  │    - Emit metrics                                             │  │
│  │    - Trigger alerts                                           │  │
│  │    - Replay messages (API/CronJob)                            │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ Metrics & Alerts
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│  OBSERVABILITY                                                       │
│  ─────────────────────────────────                                   │
│                                                                       │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐          │
│  │  Prometheus  │    │   Grafana    │    │  Alertmanager│          │
│  │  Metrics     │───→│  Dashboards  │───→│  Alerts      │          │
│  └──────────────┘    └──────────────┘    └──────────────┘          │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔑 Key Configuration Values

| Component | Setting | Value |
|-----------|---------|-------|
| **Knative Trigger** | `retry` | 5 attempts |
| **Knative Trigger** | `backoffPolicy` | exponential |
| **Knative Trigger** | `backoffDelay` | PT1S (1 second) |
| **Knative Trigger** | `timeout` | PT30S (30 seconds) |
| **DLQ Queue** | `x-message-ttl` | 604800000ms (7 days) |
| **DLQ Queue** | `x-max-length` | 50000 messages |
| **DLQ Queue** | `x-overflow` | reject-publish |
| **DLQ Replay** | `batch_size` | 10 messages |
| **DLQ Replay** | `delay_ms` | 100ms |
| **DLQ Replay** | `max_age_hours` | 24 hours |

---

**Last Updated**: 2025-01-XX  
**Version**: 1.0.0

