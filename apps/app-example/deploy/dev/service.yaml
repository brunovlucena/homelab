---
apiVersion: v1
kind: Service
metadata:
  name: "myapp-test"
  namespace: dev
  labels:
    component: "myapp"
    role: "dev"
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      nodePort: 30010
      name: http
  selector:
    component: "myapp"
    role: "dev"
